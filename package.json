{
  "name": "hello-igaming-platform",
  "version": "0.1.0",
  "private": true,
  "description": "iGaming Platform — B2B2C multi-tenant casino (AI-SDLC demo)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:fitness": "jest --config jest.fitness.config.js",
    "dev": "docker-compose up"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "@types/supertest": "^6.0.2",
    "express": "^5.2.1",
    "jest": "^29.7.0",
    "supertest": "^6.3.4",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
